---
import { classnames, display, flex, flexDirection, flexShrink, height, overflow, padding } from 'tailwindcss-classnames'
import BaseHead from '../components/BaseHead.astro'
import Header from '../components/Header.astro'
import Footer from '../components/Footer.astro'
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts'
import '@/styles/index.css'
import GetThemePreference from '@/components/GetThemePreference.astro'
import { Hero } from '@/components/page/index/Hero.tsx'

---

<script is:inline>
</script>


<html lang="en">
  <head>
    <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
  </head>
  <body class={classnames(
    height('h-screen'),
    display('flex'),
    flexDirection('flex-col'),
    overflow('overflow-hidden'),
  )}
  >
    <GetThemePreference />
    <header class={classnames(
      flexShrink('shrink-0'),
     )}
    >
      <Header />
    </header>
    <main class={classnames(
      flex('flex-1'),
      overflow('overflow-auto'),
      padding('p-6'),
    )}
    >
      <h1>🧑‍🚀 Hello, I'm Quentin!</h1>
      <Hero />
      <Footer />
    </main>
  </body>
</html>

