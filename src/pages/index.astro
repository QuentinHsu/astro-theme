---
import { type TAnimation, type TBackgrounds, alignItems, animation, backgrounds, classnames, display, flex, flexDirection, flexShrink, height, justifyContent, overflow, width } from 'tailwindcss-classnames'
import BaseHead from '../components/BaseHead.astro'
import Header from '../components/Header.astro'
import Footer from '../components/Footer.astro'
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts'
import '@/styles/index.css'
import GetThemePreference from '@/components/GetThemePreference.astro'
import { Hero } from '@/components/page/index/Hero.tsx'
import Introduction from '@/components/page/index/Introduction.tsx'

---

<script is:inline>
</script>


<html lang="en">
  <head>
    <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
  </head>
  <body class={classnames(
    height('h-screen'),
    display('flex'),
    flexDirection('flex-col'),
    overflow('overflow-hidden'),
    backgrounds('dark:bg-grid-[#1c1c1c]' as TBackgrounds, 'bg-grid-[#f3f4f6]' as TBackgrounds),
  )}
  >
    <GetThemePreference />
    <header class={classnames(
      flexShrink('shrink-0'),
     )}
    >
      <Header />
    </header>
    <main class={classnames(
      flex('flex-1'),
      overflow('overflow-auto'),
      width('w-full'),
      display('flex'),
      flexDirection('flex-col'),
      justifyContent('justify-center'),
      alignItems('items-center'),
      animation('delay-[1.2s]' as TAnimation, 'slide-enter' as TAnimation),
    )}
    >
    <container class={
      classnames(
        width('md:w-1/2', 'w-11/12'),
        height('h-full'),
        display('flex'),
        flexDirection('flex-col'),
        justifyContent('justify-center'),
        alignItems('items-center'),
      )

    }
    >
      <Introduction />
      <Hero />
      <Footer />
    </container>

    </main>
  </body>
</html>

